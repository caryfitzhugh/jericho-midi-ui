(ns test.jericho-midi.database-test
  (:require
   [test.jericho-midi.utils :as u]
   [devcards.core :refer-macros [deftest]]
   [cljs.test :refer-macros [is testing]]))

;(deftest test-expectation
;  (testing "Haven't implemented this yet..."
;    (is (= 0 1))))
