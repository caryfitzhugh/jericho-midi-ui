(ns jericho-midi.locales)

(def dictionary
  {:en {
    :missing "Fallback for missing: en"
  }
  :pirate {
    :missing "Fallback for missing: pirate"
  }
  ;; http://www.tourcanada.com/comedy.htm
  :en-CA {
    :missing "Fallback for missing: en-CA"
  }
})
